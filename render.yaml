services:
  - type: web
    name: odic-esa
    runtime: docker
    dockerfilePath: ./Dockerfile.prod
    autoDeploy: false
    envVars:
      - key: AI_BACKEND
        value: anthropic
      - key: ANTHROPIC_API_KEY
        sync: false
