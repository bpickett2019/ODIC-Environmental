name: Deploy to Railway

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to Railway
        uses: anza-labs/railway-action@main
        with:
          service: ""
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
